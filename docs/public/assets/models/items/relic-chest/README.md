# Relic Chest Runtime Asset

Codex should generate the final articulated runtime asset here:

```text
relic-chest.glb
```

Do not place the source `.blend`, `.fbx`, `.obj`, or `.mtl` files in this public folder.
