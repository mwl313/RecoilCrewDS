# Recoil Crew — Relic Chest Source Pack

Drop the contents of this archive into the root of the `RecoilCrewDS` repository while on the `quality-improvement` branch.

The pack intentionally includes only the Closed/Open chest sources and reference renders required for the articulated relic chest task.

It contains no gold-ingot geometry.

Expected final runtime output after Codex finishes:

```text
public/assets/models/items/relic-chest/relic-chest.glb
```

Source assets remain under `docs/relics/source/` and therefore are not shipped by Vite as public runtime assets.
